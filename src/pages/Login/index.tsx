import React from "react";
import { Button } from "antd"; // 引入 Ant Design 的按钮组件

export default function Login() {
  return (
    <div>
      登录页
      <Button type="primary">登录</Button> {/* 这是一个带有主题色的按钮 */}
    </div>
  );
}
